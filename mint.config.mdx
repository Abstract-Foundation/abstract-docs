import { defineConfig } from 'mintlify';

export default defineConfig({
  name: 'Abstract Documentation',
  // Logo showing in the top left
  logo: '/logo.svg',
  // By default the navigation will be on the left, you can change it to 'right'
  navigation: {
    location: 'left',
    // Explicitly disable the chat feature
    chat: false
  },
  // Additional configuration options...
});